# 技能环境变量加载规范

技能支持安装运行和项目内研发两种形态。运行时按以下优先级加载配置：

1. 已存在的进程环境变量（最高优先级）。
2. 技能根目录下的 `.env`。
3. 项目目录下的 `.env`（从当前工作目录向上查找，直到项目根目录）。

同名变量由优先级更高的来源覆盖，未被覆盖的变量继续保留。`.env.example` 只用于声明技能所需变量和填写示例，不作为运行时配置读取，也不得包含真实凭据。

技能安装后，可将技能目录的 `.env.example` 复制为技能目录 `.env`；研发阶段可直接在项目目录 `.env` 中集中配置。
