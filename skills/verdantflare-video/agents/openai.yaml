interface:
  display_name: "VerdantFlare Video"
  short_description: "Route video requests or run explicit SD2 tasks"
  default_prompt: "Use $verdantflare-video to make a video from this brief using the default route unless I specify a model."
